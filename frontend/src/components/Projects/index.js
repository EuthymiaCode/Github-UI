import React from 'react'

import { Container, Input, NewProjectButton } from './styles'

function Projects() {
  return (
    <Container>
      <header>
        <Input placeholder='is:open sort:created-desc' />

        <NewProjectButton>ejfneln</NewProjectButton>
      </header>
    </Container>
  )
}

export default Projects
